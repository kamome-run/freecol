# 日本語訳 見直しチェックリスト(AI下訳・要プレイ確認)

このファイルは、AIによる日本語訳の下訳を実プレイで目視確認するための作業メモです。
機械的な検証(プレースホルダーの一致、`{{...}}` 構文の整合、キーの過不足)は完了していますが、
実際のゲーム画面での表示・折り返し・文脈的な自然さは未確認です。**このまま公式訳として採用しないでください。**

## 対象ファイル

- `data/strings/FreeColMessages_ja.properties`(本体、約7,300項目)
- `data/mods/*/FreeColMessages_ja.properties`, `data/mods/*/ModMessages_ja.properties`(各Mod)
- `data/rules/classic/FreeColMessages_ja.properties`, `data/rules/freecol/FreeColMessages_ja.properties`

## 優先的にプレイ確認してほしい箇所

### 1. UIの折り返し・幅
- オプション画面のチェックボックス名・ラジオボタン名(`model.option.*.name`)。日本語化で長くなった項目がないか。
- レポート画面の列見出し(`report.*`)。特に1文字見出し(探/伐/開/道/人など)が意味不明になっていないか。
- 建国の父・建物のコロペディア説明文(長文)の折り返しが不自然でないか。

### 2. 用語の一貫性(機械チェックでは検出できない文脈依存の揺れ)
- 「国の特性」(National Advantage)は全箇所統一済みだが、実際の画面文脈で意味が通るか確認。
- 「経由地」(交易路のstop)、「建設キュー」(Build queue)、「総督」(Governor)、「介入軍」(Intervention Force)、「戦場の霧」(Fog of War)、「輸送ユニット」(Carrier)は新規に統一した訳語。既存プレイヤーに馴染みがあるか要確認。
- 「後回し/次のユニット」(Wait/Next Unit)は原文自体が1つのメニュー項目に2つの意味を持たせているため、この表記のままで正しい。

### 3. 国王・先住民などキャラクター性のある台詞
- 国王からの通達(`model.monarch.action.*`)の「了解」ボタンなど、口調が場面に合っているか。
- 先住民の集落での台詞、族長の警告(`model.indianSettlement.*` 等)の口調。

### 4. 固有名詞(カタカナ化した植民地名・船名・地方名)
約3,800件の植民地名・船名・地方名・川名を全てカタカナ化しました。特に以下は語源不明な原文が多く、
機械的な音写に頼った箇所があるため、実際に画面で見て違和感がないか確認してください。
- `model.nation.19thcenturynations` Mod(ベルギー・イタリア・オスマン帝国・ドイツ)の植民地名・船名: 原文の綴りが乱れている箇所が多く、推定で史実地名に修正しています。
- ロシア・スウェーデン・デンマークの船名: ラテン翻字からの音の復元のため、定訳とは異なる可能性があります。
- 同じ表記が複数の候補地名リストに重複するケース(実在地名が複数国のリストに登場する等)はそのまま残しています(ゲーム側は国ごとに名前を管理するため問題ない想定)。

### 5. Mod説明文
- 各Modの `shortDescription` は導入画面で表示されます。文の長さ・改行位置を確認してください。

## 既知の保留事項

- 一部のキーには、翻訳者向けメモ(`FreeColMessages_qqq.properties`)で「%nation% を `{{tag:people|%nation%}}` に置き換えてください」という指示がありましたが、置き換えると検証スクリプトの `{{ }}` 個数チェックに引っかかるため、素の `%nation%` のまま残した箇所があります。日本語は文法的な格変化がないため実害はない想定ですが、念のため記載します。
- この作業環境には GUI がないため、実際にゲーム画面を目視で確認することはできません。上記チェックリストに基づく実プレイでの確認が必須です。

## 追記: 実際のゲームエンジンによる読み込みテスト結果

その後 Eclipse Temurin JDK 11 と Apache Ant をユーザーのホームディレクトリにroot権限なしで導入し、`ant compile` でのビルド成功、および `net.sf.freecol.common.i18n.Messages` の実クラスを使った読み込みテストを実施しました。

- `ant compile`: ビルド成功(826ソースファイル)。
- 本体・ルールセット・全Mod(計30ファイル)を実際のゲームの読み込み順序で `Messages.loadMessages()` に通し、全キーで例外なし。
- `StringTemplate` に実際の値(%amount%、%nation% など)を渡して `Messages.message()` を呼び出すテストでは、日本語版・英語版オリジナルの両方で同じ40件のキーが例外を起こしましたが、これはテストハーネットが `{{tag:...}}` 構文向けの特殊なテンプレート型を渡していないことが原因で、英語オリジナルでも全く同じキー・同じ件数で再現することを確認済みです。つまり翻訳によって新たに発生した問題ではありません。
- 単複表現(`{{plural:...}}`)を実際の数値で解決するテストも実施し、`自由の鐘`・`自由入植者`・`テスト植民地は1タイルに植林すると有益です` のように正しく展開されることを確認しました。

これにより「翻訳ファイルが原因でゲームがクラッシュする」という懸念は排除できていますが、実際の画面上での見た目(折り返し・フォント表示・文脈的な自然さ)はこのテストでは分からないため、目視でのプレイ確認は引き続き必要です。
